const form = document.querySelector('#form')

form.addEventListener('submit', (e) => {
    e.preventDefault()

    try{
        StartLoading()
        // pegar o form
        // loading
    } catch(error) {

    }
})